package org.aspose.slides.foss.internal.pptx;

/**
 * Internal data holder for a heading pair read from app.xml.
 *
 * @param name  the heading name
 * @param count the number of parts in the group
 */
public record HeadingPairData(String name, int count) {
}
