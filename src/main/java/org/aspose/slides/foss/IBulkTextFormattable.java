package org.aspose.slides.foss;

/**
 * Represents an object with the possibility of bulk setting child text elements' formats.
 */
public interface IBulkTextFormattable {

    /**
     * Sets the text format for all child text elements.
     *
     * @param source the format source to apply
     */
    void setTextFormat(Object source);
}
